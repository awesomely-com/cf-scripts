export interface CountdownTimerReturn {
	days: number;
	hours: number;
	minutes: number;
	seconds: number;
}

export interface CountdownElements {
	countdown: string;
	countdownContainer: string;
	banner: string;
}
